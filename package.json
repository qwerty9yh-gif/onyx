{
  "name": "onyx-pos-system",
  "version": "1.0.0",
  "private": true,
  "description": "ONYX POS System — premium point of sale, inventory, invoicing and analytics.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "start": "node apps/api/dist/index.js",
    "db:migrate": "prisma migrate deploy --schema prisma/schema.prisma",
    "db:push": "prisma db push --schema prisma/schema.prisma",
    "db:verify": "npm run verify-schema --workspace=apps/api",
    "db:seed": "tsx prisma/seed.ts",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "prestart": "npm run db:push && npm run db:generate && npm run db:verify"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
